# VeriStream：面向因果长视频理解的自主证据检索智能体

## 摘要

长视频包含的视觉信息远超多模态模型单次上下文容量。均匀增加帧数既会引入大量冗余，又会让久远历史
与当前视觉竞争有限注意力。我们在冻结 Qwen3-VL-8B 上发现：从 Recent4 扩大到 Recent32，Backward
提高 3.43 个百分点，但 Realtime 下降 4.90 个百分点；将历史压缩为 action-fact text memory 并保留
Recent4，则 Backward 提高 8.17 个百分点而 Realtime 仅下降 1.82 个百分点。这表明核心问题不是“放入
更多帧”，而是“按问题获取少量可追溯证据，同时保护当前视觉”。

据此，我们提出 training-free 长视频智能体 VeriStream。系统离线构建视频隔离的两级持久记忆：L0
保存原始帧、时间戳与 CLIP embedding；L1 保存带 L0 provenance 的时间块概览和经语义门控的局部
事件。问题到来后，Planner 将问题拆成原子 EvidenceNeed；受约束的 LLM Controller 自主判断当前证据
是否充分，并循环调用 Search、Inspect、Expand、Compare 或 Finish。程序执行器只负责校验因果边界、
memory ID、状态转移与预算，不替模型选择工具。最终模型接收已验证的历史证据与 exact Recent4。

本文在 OVO-Bench 的 Backward 631 题和 Realtime 837 题上评测。现有完整预实验说明文本记忆是强基线，
也暴露了自由 Agent 路由错误、相似度检索强制返回无关项、结构化输出截断和视觉变化语义混淆等失败
模式。我们逐项给出对应设计、可复现实验协议及声明边界；尚未完成的同基座纯 Uniform32 与最终自主
Controller 全量结果不以不等价数据替代。

论文方法图使用经过逻辑约束的
[GPT Image 2 绘图提示词](docs/veristream/gpt_image2_method_figure_prompt.md)生成。生成后应保存为
`figures/veristream_method.png`，并按照提示词末尾 Checklist 检查自主工具闭环、Recent-4 独立通道、
L0/L1 provenance 和 temporal evidence graph 后再插入本文，避免提交未经审核的占位图。

## 1. 问题建模

给定视频 $V=(f_1,\ldots,f_T)$、问题 $q$、选项 $o$ 与问题时刻 $t_q$，系统只能访问因果前缀
$V_{\le t_q}$，并输出选项答案。Backward 问题依赖过去发生的事件、位置或顺序；Realtime 问题依赖
$t_q$ 附近的动作、状态、属性、空间关系或文字。我们不更新任何模型参数，核心编排逻辑全部在仓库中
实现，不调用现成视频 Agent 框架。

方法需满足五项约束：

1. **因果性**：索引、检索、回看与回答均不得访问 $t_q$ 之后的帧；
2. **自主性**：模型根据已有证据决定下一工具或停止，而不是固定调用链冒充 Agent；
3. **可追溯性**：语言记忆必须能回指原始帧与时间区间；
4. **可拒绝性**：全库无关时允许返回空集，不强制提供“最相似”证据；
5. **可审计性**：记录 Planner、工具、视觉帧、repair、预算与停止原因。

## 2. 预实验：从现象推出设计

### 2.1 更多原始帧造成历史与当前冲突

在相同 Qwen3-VL-8B、1 FPS、因果边界和评分规则下：

| 单轮输入 | Backward | Realtime | BR 平均 |
| --- | ---: | ---: | ---: |
| Recent4 | 53.92 | **81.47** | **67.70** |
| Recent8 | 55.25 | 79.59 | 67.42 |
| Recent16 | 55.06 | 77.80 | 66.43 |
| Recent32 | **57.35** | 76.57 | 66.96 |
| Recent64 | 56.15 | 75.62 | 65.89 |

Recent32 虽提高历史覆盖，却显著损害当前感知；Recent64 连历史性能也不再增长。因此最终回答不应直接
拼接大量历史帧。

**设计结论 C1：双通道。** 当前视觉固定为与强基线完全一致的 exact Recent4；历史进入独立记忆与
工具路径，仅把少量问题相关证据送入最终上下文。

### 2.2 文本记忆优于继续堆帧

| 历史表示 | 历史选择 | Backward | Realtime | BR 平均 |
| --- | --- | ---: | ---: | ---: |
| 无历史记忆 | - | 53.92 | **81.47** | 67.70 |
| Action-fact text | Uniform16 | **62.09** | 79.65 | **70.87** |
| Action-fact text | CLIP-TopK16 | 61.93 | 78.36 | 70.15 |

语言压缩显著提高 Backward，说明历史应以语义单元保存。然而纯文本会丢失颜色、OCR、空间和细微状态，
也可能把模型推断写成事实。

**设计结论 C2：文本定位、原图核验。** L1 文本负责低成本定位；需要视觉细节时通过 provenance 回读
L0，而不是无条件相信摘要。

### 2.3 相似帧不等于证据

Uniform 提供覆盖，CLIP-TopK 提供外观相似性，但直接追加这些帧未稳定超过 Recent4。CLIP 擅长物体和
场景匹配，却不能单独判断动作、状态转移和先后关系；纯 Top-K 还会在完全无关的历史中强制返回第一名。

**设计结论 C3：Proposal 与事实分离。** CLIP 只提出“哪里可能变化”，冻结 VLM 才判断“发生了什么”；
检索设绝对门槛，允许零召回。

### 2.4 自由 Agent 并不自动可靠

早期工具化完整实验为 Backward 58.19、Realtime 79.40、BR 平均 68.79，仍低于 action-fact 强基线。
日志显示三类原因：Planner 会把过去式问题改写为 current-only；Controller 重复 Search、很少稳定执行
Expand/Compare；缺乏证据时仍可能提前 Finish。

**设计结论 C4：自主决策、受约束执行。** LLM 保留工具选择和充分性判断；程序只实施不可绕过的时间
语义规则、合法动作集合、ID 检查、子预算和保守 fallback。确定性 Controller 仅作为消融，不作为题目
要求的主系统。

### 2.5 结构正确不代表语义正确

早期索引把 overview 与多个 proposal assessment 塞入 256-token 上限，54,301 个 proposal 中主调用
初始有效仅 304 个（0.56%），大量 JSON 在数组中途截断。使用 JSON prefill、完整括号停止、动态 ID
骨架、2,048-token 索引故障边界和 targeted repair 后，分层 24 题达到：

| 结构指标 | 结果 |
| --- | ---: |
| Proposal | 230 |
| 主调用初始有效 | 210/230 (91.3%) |
| Repair 后有效 | 230/230 (100.0%) |
| Repair calls / recovered | 10 / 20 |
| Unresolved | 0 |

但当时 230/230 全被接纳，spot-check 发现 scene cut 和 camera motion 被误写成对象动作。Repair 只修复
缺失 ID、非法 JSON 或层级不一致，不修复语义判断。

**设计结论 C5：层级语义门控。** 先区分 local semantic event、global visual change 和 no meaningful
change，再决定是否允许进入可检索证据。

## 3. VeriStream 方法

### 3.1 感知与推理解耦

系统使用相同基座模型形成两个角色，但不共享对话上下文：

- **Perception role** 只观察索引帧或工具回看的原图，输出可见事实与 provenance；
- **Reasoning role** 进行 EvidenceNeed 规划、工具选择、充分性判断和最终多选回答。

角色可由同一权重实例复用，也可在同一 GPU 加载两个独立实例。正式配置采用独立实例以避免频繁切换
上下文并提高吞吐。解耦的主要风险是感知摘要压缩损失，因此所有 L1 节点保留 L0 链接，Inspect 和
Compare 可现场重新感知；最终推理看不到未通过 admission 的文本。

### 3.2 持久记忆与生命周期

系统只有 L0/L1 两级持久 memory：

- **L0 RawFrameMemory**：frame ID、时间戳、原始图像、CLIP embedding、所属 block；
- **L1 OverviewMemory**：12 秒时间块的实体、动作、可见文字与概览；
- **L1 TransitionMemory**：成对 before/after 帧对应的局部动作或状态变化。

Proposal 是构建 TransitionMemory 的临时候选，不是第三层 memory。每条 L1 节点都记录 video ID、时间
范围和 L0 frame IDs。相邻 Overview、父子 Overview-Transition 和相邻 Transition 组成时间证据图。

记忆在视频首次出现或 cache 不兼容时构建；相同视频、相同边界和相同 schema 可复用不变 block。
模型无权把最终答案或其他视频经验写回持久层，从源头抑制确认偏差。问题级 Working Evidence 回答后
立即删除。持久 cache 按 video ID 物理隔离；schema、视频边界、采样率或索引配置不匹配时拒绝加载，
删除视频 cache 即完成显式遗忘。被语义门拒绝的 proposal 只保留审计记录，不进入搜索库。

### 3.3 Coverage-Change Index

每个 12 秒历史 block 均匀保留 4 个 coverage frames，保证静态场景、持续动作和 OCR 不因“变化不大”
而消失。对 CLIP embedding $e_t$，在跨度 $s\in\{1,2,4\}$ 上计算：

$$d_s(t)=1-\cos(e_{t-s},e_t).$$

多跨度用于覆盖瞬时变化、短动作和较慢状态转移。由于长跨度原始距离天然偏大，每个跨度独立估计：

$$m_s=\operatorname{median}(d_s),$$

$$\sigma_s=\max\left(1.4826\operatorname{MAD}(d_s),
\frac{Q_{0.75}(d_s)-Q_{0.25}(d_s)}{1.349}\right),$$

$$z_s(t)=\frac{d_s(t)-m_s}{\sigma_s}.$$

这里 MAD 是 $\operatorname{median}(|d_s-m_s|)$；1.4826 将其变为正态分布下的稳健标准差估计。每个
after frame 只保留最大 $z_s$ 的跨度，并要求原始距离 $\ge0.05$、稳健显著性 $\ge0.5$；随后经过局部
峰值和 2 秒 temporal NMS，每 block 最多产生 2 个 proposal。它比增加均匀抽帧密度更有针对性：新增
成本集中在可能的变化位置，且候选仍需语义核验。

### 3.4 Hierarchical Semantic Gate

Coverage、proposal pair 与局部上下文去重后组成至多 8 帧的 block pack。Perception role 在一次调用中
联合生成 Overview 及所有 proposal 的 paired assessment，避免每个候选单独调用：

```text
local_semantic_event -> object_action | state_change -> searchable evidence
global_visual_change -> scene_cut | camera_motion    -> navigation only
no_meaningful_change                                 -> reject
```

Validator 要求 proposal ID 一一对应、父类与细类一致、before/change/after 非空。缺失或非法项触发一次
targeted structural repair；repair 的调用数、恢复数和失败数单独记录，并可用
`--disable-proposal-repair` 消融。

### 3.5 EvidenceNeed 与 Temporal Evidence Graph

Reasoning role 只根据问题和选项生成原子 Need：scope、evidence type、检索描述、实体、动作、时间关系、
优先级与 scope confidence。过去式、before/after、first/last、again 和计数等关键词由程序保留原始时间
语义，不能被 Planner 改写为 current-only。没有关系边且所有 Need 均为高置信 current-only 时，系统
直接使用与 Recent4 baseline 完全相同的帧和 prompt；低于 0.7 的 current scope 扩展为 both。该分数
只是模型自报的路由置信度，需用 ECE 审计，不是答案置信度。

时间顺序问题拆为两个可独立定位的事件和一条显式关系边：

```text
N1 = observable event A
N2 = observable event B
R1 = before(N1, N2)
```

两端分别召回和核验，再根据 Transition peak time 或不重叠区间判断关系；重叠或缺失保持 uncertain，
不把一个模糊 query 的两个 Top-K 候选强行解释为两件事。

### 3.6 Search：可拒绝的混合召回

Search 面向 L1 节点，而不是按固定帧数搜索。每个 historical/both Need 使用：

- BGE cosine：语义召回；
- BM25：实体、动作和 OCR 词面召回；
- CLIP text-image：仅 attribute、spatial、state 等视觉外观 Need 启用。

候选必须至少满足 BGE $\ge0.25$、BM25 $\ge0.10$ 或条件式 CLIP $\ge0.20$，否则返回空集。门槛后的
候选用 reciprocal rank fusion 合并：

$$R(c,n)=\sum_{r\in\mathcal R_n}\frac{w_r}{10+\operatorname{rank}_r(c)},$$

其中 CLIP 权重为 0.25。RRF 融合各检索器的相对名次，避免直接相加不可比的 cosine 与 BM25 原始值。

为减少重复，候选集合 $S$ 迭代最大化：

$$U(c\mid S)=G(c\mid S)-0.25\max_{s\in S\cup S_{prev}}D(c,s),$$

$$D(c,s)=0.5\,\operatorname{sim}_{text}+0.25\,\operatorname{sim}_{visual}
+0.25\,\operatorname{IoU}_{time}.$$

$G$ 是对尚未覆盖 Need slot 的优先级加权增益。第一条候选没有集合内冗余惩罚，但仍必须通过绝对门槛；
后续候选只在 utility $>0$ 且 metadata budget 可容纳时加入。下一轮 Search 自动排除已见 memory。

### 3.7 自主工具循环

Controller 每轮看到 EvidenceNeed、关系边、Working Evidence、最近工具结果和剩余预算，并只允许输出：

- **SearchMemory**：Need 尚无可靠时间锚点时搜索 L1；
- **ExpandMemory**：已有锚点后沿 before/after/parent/children 图边导航；
- **InspectMemory**：文本缺少属性、空间、状态、OCR 或时间细节时回读 L0；
- **CompareMemory**：两个已定位节点需要顺序、状态、身份或空间比较时联合观察；
- **FinishRetrieval**：重要 Need 已充分覆盖或无有效动作时停止。

模型自主选择上述动作和 Finish。执行器拒绝不存在的 ID、未定位节点上的 Inspect、越过因果边界的帧、
重复越预算动作和非法 schema。默认最多执行 4 次 Search/Inspect/Expand/Compare，其中 Search 不超过
2 轮、图导航不超过 2 次、视觉动作不超过 2 次、历史视觉帧不超过 8；Finish 不计费。`max-tool-actions`
不是“最多只用几条证据”，而是防止循环失控并建立可复现的成本上界。预算耗尽仍未满足时显式记录
unresolved，而不是伪造充分证据。

### 3.8 最终 Evidence Admission

attribute/spatial/state/OCR 和时间图端点必须经过 Inspect 或 Compare；普通历史事件可使用经 semantic
gate、绝对检索门槛和 provenance 检查后的 L1 text。partial、rejected、unresolved relation 不进入最终
上下文。最终 VLM 同时看到预算内历史证据和 exact Recent4，并以 Recent4 回答当前动作与状态，以历史
证据回答过去事件与顺序。

## 4. 实验设置

### 4.1 数据集与任务构成

选择 OVO-Bench 是因为它在同一因果视频设置中同时包含历史回溯和实时感知，直接对应本文发现的冲突。
主实验只使用 Backward 和 Realtime：

| 维度 | 任务 | 数量 |
| --- | --- | ---: |
| Backward | EPM / HLD / ASI | 297 / 186 / 148 = 631 |
| Realtime | STU / OJR / ATR / FPD / ACR / OCR | 178 / 184 / 116 / 101 / 109 / 149 = 837 |
| 合计 | 9 类 | 1,468 |

每个任务先独立计算准确率；Backward 和 Realtime 分别对其任务宏平均。本文 `BR 平均` 定义为二者算术
平均，不包含 Forward。所有方法采用相同 Qwen3-VL-8B、1 FPS、因果边界和错误计 0 规则。

### 4.2 Baseline 与当前结果边界

题目要求的主 baseline 是同一 8B 基座、因果前缀 Uniform32、单轮直接问答。仓库已有完整的 2B
Uniform32，但它不是同基座，不能用于主对比；8B 纯 Uniform32 必须按 README 命令补跑。当前可复现的
同基座诊断结果如下：

| 方法 | Backward | Realtime | BR 平均 | 状态 |
| --- | ---: | ---: | ---: | --- |
| Required Uniform32, 8B, single-turn | 待运行 | 待运行 | 待运行 | 提交前必补 |
| Recent4, 8B, single-turn | 53.92 | 81.47 | 67.70 | 已完成 |
| Action-fact + Uniform16 + Recent4 | **62.09** | 79.65 | **70.87** | 已完成强基线 |
| 早期工具化框架 | 58.19 | 79.40 | 68.79 | 已完成诊断 |
| 当前自主 Controller + semantic gate | 待运行 | 待运行 | 待运行 | schema-6 全量未完成 |

因此目前不能声称最终 VeriStream 超过 action-fact，也不能用“框架更复杂”替代数值。已完成结果的合理
结论是：Memory 确实有效，但不可靠路由和工具编排会抵消收益；当前方法的每个模块都针对日志中可观察
的失败模式，最终收益必须由相同样本 paired 评测确认。

### 4.3 消融设计

| 消融 | 所回答的问题 |
| --- | --- |
| Uniform32 single-turn vs VeriStream | 自主工具与 Memory 相对题目 baseline 的净收益 |
| 无 Memory / L1 text-only / L1+L0 inspect | 持久记忆、压缩损失和现场感知各自作用 |
| Coverage-only / Change-only / Both | 时间覆盖与变化定位是否互补 |
| span 1 vs raw 1/2/4 vs normalized 1/2/4 | 多尺度及 per-span MAD normalization 是否必要 |
| semantic gate on/off | 过滤镜头运动是否提高证据 precision |
| proposal repair on/off | 结构恢复收益与额外调用成本 |
| BGE / BM25 / conditional CLIP | 各召回分支贡献 |
| rank-only / complementary utility | Need coverage 与冗余控制 |
| autonomous / deterministic controller | 自主性、稳定性、成本和准确率权衡 |
| tool budget 2 / 4 / 6 | 准确率与调用成本前沿 |
| composite order query / temporal graph | 显式双事件关系是否改善 ASI |

主方法与 baseline 使用逐样本、按 task 分层的 paired bootstrap，至少 10,000 次重采样，报告 Backward、
Realtime、BR 平均差值和 95% 置信区间。若区间跨 0，则不宣称显著提升。

### 4.4 Agent 与 Memory 专项指标

除最终准确率外报告：Planner historical recall、current precision、ECE；每种工具调用次数、非法调用率、
重复率、Finish 时未满足 Need 比例、预算耗尽率、历史视觉帧和 wall-clock；cache hit、索引调用和生成
token；proposal 数、assessment completion、repair recovery、semantic-gate precision/recall/F1、检索
Recall@K 和时间关系正确率。

100-pair semantic audit 使用 80 个模型 proposal 与 20 个 low-change control，由至少两名标注者独立
标注 local/global/none、时间对齐和 before/change/after，并报告 Cohen's kappa。该审计测 gate 精度，
不能冒充 proposal detector recall；后者必须先对完整时间块人工标注事件，再判断是否被 proposal 覆盖。

## 5. 问题、风险与控制

- **错误记忆**：VLM 可能把推断写成事实。对策是层级 semantic gate、L0 provenance、视觉 Need 强制
  Inspect/Compare，以及不把最终答案写回持久层。
- **冗余记忆**：相邻 block 可能重复同一事件。对策是 temporal NMS、相邻节点图和基于文本、视觉、
  时间重叠的 utility 去冗余。
- **记忆污染**：跨视频经验可能错误迁移。对策是 video ID 命名空间、每题清空 Working Evidence、
  cache 配置严格匹配和禁止模型自主持久写入。
- **压缩损失**：Overview 可能遗漏颜色、文字和空间细节。对策是 L1 只定位、按 Need 回看 L0。
- **自主停止失误**：Controller 可能过早 Finish 或重复调用。对策是完整 trace、Need 状态、硬预算、非法
  调用反馈和确定性 controller 对照；不能靠 prompt 声明已解决。
- **视觉变化混淆**：CLIP distance 同时响应动作、镜头运动和场景切换。对策是多跨度 proposal 只做
  高召回定位，语义门控决定 admission，并用人工 audit 标定。
- **解码失败**：个别 MP4 的 decord EOF 会回退 torchvision；设置 `DECORD_EOF_RETRY_MAX=20480`，记录
  backend，仍失败则计 0 而不静默丢题。

## 6. 复现与提交检查

代码入口、数据下载、八卡命令和 cache 规则见 `README.md`。当前 `CoverageChangeIndex` 使用 schema 6；
旧 cache 缺少 hierarchical admission、rejected provenance 和 gate status，不能用于当前主实验。相同
schema-6 index 可通过 `--index-cache-dir` 只读复用于 question-time 消融。

提交前必须完成以下检查：

1. 跑完 8B Uniform32 的 631 Backward + 837 Realtime，并填入主表；
2. 跑完 `--controller-mode llm` 的当前 schema-6 全量实验，不用确定性结果冒充自主 Agent；
3. 完成 100-pair 双人 semantic audit，并明确 gate 与 detector recall 的区别；
4. 对主方法和 Uniform32 执行 paired bootstrap；
5. 报告准确率、工具成本、错误数和运行时间，保留完整 config 与 trace；
6. 从干净环境执行数据校验、`--help`、单元测试和最小样例。

## 7. 结论

预实验支持的核心判断是：长视频理解的瓶颈不是抽帧数量本身，而是历史证据与当前视觉竞争，以及“相似
内容”与“可回答证据”之间的差距。VeriStream 因此把当前视觉固定为 exact Recent4，把历史组织为视频
隔离、可追溯、可拒绝的 L0/L1 memory，并让 LLM 在受验证的工具接口上自主决定获取何种证据及何时
停止。该设计满足 training-free、多模态工具、持久 Memory 和感知/推理解耦要求；是否超过同基座均匀
抽帧 baseline，仍以待完成的全量 paired 实验为唯一判断依据。
