# Shared dependencies for the SimpleStream codebase.
# Install torch / torchvision separately for your platform first
# (see https://pytorch.org/get-started/locally/), then install requirements-qwen3.txt.

# Video decoding
av>=14.4.0

# Image / array utilities
Pillow>=10.0.0
numpy>=1.24.0

# Progress bars
tqdm>=4.65.0

# Qwen3-VL preprocessing helper
qwen-vl-utils==0.0.14
